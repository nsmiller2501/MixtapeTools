# `/setup-researcher-skills` — Research Tracker Conventions

Configures a research project for planning skills by writing the tracker substrate, artifact boundaries, labels, lifecycle, and markdown task-bridge conventions that downstream agent skills should follow.

Use before adapting or running researcher-oriented planning skills such as `/grill-with-docs`, `/to-spec`, `/to-tickets`, `/wayfinder`, `/triage`, or `/implement`.

---

*Local MixtapeTools skill.*
